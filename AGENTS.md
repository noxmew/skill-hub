# 仓库协作约定

## 仓库定位

这是一个个人自用的通用 skills 仓库，用来沉淀可复用的工作流、提示词、脚本和参考资料。仓库不默认绑定任何特定模型、Agent、编辑器或运行平台。

- 优先保证实用、清晰、易维护；避免为了“完整”引入不必要的框架、依赖和抽象。
- 核心 skill 应尽量保持平台无关；平台特有能力必须明确标注，不伪装成通用能力。
- 默认使用中文编写说明，代码、命令、文件名和行业术语保留其自然语言形式。

## 目录约定

每个 skill 放在独立目录中：

```text
skills/<skill-name>/
├── SKILL.md              # 必需：入口、触发条件和执行说明
├── scripts/              # 可选：可重复执行的自动化脚本
├── references/           # 可选：按需读取的详细资料
├── assets/               # 可选：模板、示例和静态资源
└── adapters/             # 可选：特定平台或工具的适配层
```

- skill 目录名使用小写 kebab-case，并与 `SKILL.md` 中的 `name` 保持一致。
- 只有被 skill 实际使用的内容才放入 `scripts/`、`references/` 或 `assets/`。
- 跨文件引用使用相对路径，不写入只在某台机器上成立的绝对路径。
- 平台专属实现放入 `adapters/<platform>/`；如果没有独立文件，也要在相关段落清楚注明适用平台。
- 仓库级说明放在根目录；单个 skill 的具体规则放在其自己的 `SKILL.md` 中。

## Skill 编写原则

- `SKILL.md` 必须包含有效的 YAML front matter，至少声明 `name` 和 `description`。
- `description` 要写清楚“何时使用”，必要时也写清楚“何时不要使用”，让触发边界可判断。
- 正文使用直接、可执行的指令，明确输入、步骤、产物和完成条件。
- 用能力描述代替特定产品的工具名，例如描述“搜索网页”或“执行命令”；只有适配层可以依赖具体工具接口。
- 保持入口文件精炼：核心流程留在 `SKILL.md`，较长背景资料拆到 `references/` 并注明读取时机。
- 优先复用已有脚本、模板和资源；不要在指令中重复粘贴大段可维护代码。
- 示例应贴近实际个人工作流，不为展示而堆砌示例。
- 涉及破坏性操作、外部写入、凭据或费用时，明确前置检查和安全边界。

## 脚本与依赖

- skill 需要编写脚本或工具时，默认使用 Python；只有现有技术栈或任务特性明显更适合其他语言时才采用其他语言。
- 使用 Python 的 skill，必须在其 `SKILL.md` 中说明如何使用 `uv` 创建和使用独立虚拟环境，至少包含 `uv venv` 以及所需的依赖安装或同步命令。
- 脚本应尽量确定性、可重复运行，并在失败时返回非零退出码和可理解的错误信息。
- 优先使用系统已有工具或仓库既有依赖；新增依赖前说明必要性。
- 不提交密钥、令牌、Cookie、个人路径、私有服务地址或真实敏感数据。
- 临时文件和生成物应写入明确的临时目录，并及时清理或加入 `.gitignore`。
